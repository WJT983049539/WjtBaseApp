package com.ping.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.privatee.wjtbaseapp.Bean.TestStudent.TwoListBeanConverter;
import java.util.List;

import com.privatee.wjtbaseapp.Bean.TestStudent;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TEST_STUDENT".
*/
public class TestStudentDao extends AbstractDao<TestStudent, Void> {

    public static final String TABLENAME = "TEST_STUDENT";

    /**
     * Properties of entity TestStudent.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", false, "ID");
        public final static Property StudentName = new Property(1, String.class, "StudentName", false, "STUDENT_NAME");
        public final static Property ListBeans = new Property(2, String.class, "listBeans", false, "LIST_BEANS");
    }

    private final TwoListBeanConverter listBeansConverter = new TwoListBeanConverter();

    public TestStudentDao(DaoConfig config) {
        super(config);
    }
    
    public TestStudentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TEST_STUDENT\" (" + //
                "\"ID\" TEXT," + // 0: id
                "\"STUDENT_NAME\" TEXT," + // 1: StudentName
                "\"LIST_BEANS\" TEXT);"); // 2: listBeans
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TEST_STUDENT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TestStudent entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String StudentName = entity.getStudentName();
        if (StudentName != null) {
            stmt.bindString(2, StudentName);
        }
 
        List listBeans = entity.getListBeans();
        if (listBeans != null) {
            stmt.bindString(3, listBeansConverter.convertToDatabaseValue(listBeans));
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TestStudent entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String StudentName = entity.getStudentName();
        if (StudentName != null) {
            stmt.bindString(2, StudentName);
        }
 
        List listBeans = entity.getListBeans();
        if (listBeans != null) {
            stmt.bindString(3, listBeansConverter.convertToDatabaseValue(listBeans));
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public TestStudent readEntity(Cursor cursor, int offset) {
        TestStudent entity = new TestStudent( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // StudentName
            cursor.isNull(offset + 2) ? null : listBeansConverter.convertToEntityProperty(cursor.getString(offset + 2)) // listBeans
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TestStudent entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setStudentName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setListBeans(cursor.isNull(offset + 2) ? null : listBeansConverter.convertToEntityProperty(cursor.getString(offset + 2)));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(TestStudent entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(TestStudent entity) {
        return null;
    }

    @Override
    public boolean hasKey(TestStudent entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
